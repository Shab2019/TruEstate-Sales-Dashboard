# TruEstate Retail Backend

Node.js + Express backend that loads the provided retail sales CSV into memory and exposes a single `/api/sales` endpoint supporting search, filters, sorting and pagination, plus `/api/sales/filters` for filter options.
